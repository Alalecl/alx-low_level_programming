#include "main.h"
#include <stdio.h>

/**
 * main - checks for digit 
 * return 1 if trur
 * return 0 otherwise
 */

int _isdigit(int c);
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
